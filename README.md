# Gunicorn scripts

## Files

* gunicorn.service: a systemd service file (stored under the /etc/systemd/system/ directory)
* gunicorn.socket: used to check the status of the process to see whether gunicorn is able to be started correctly 
* gunicorn.start: used to start gunicorn

